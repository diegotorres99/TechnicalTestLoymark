@{
    ViewData["Title"] = "";
}

<!-- Table -->
<div class="row mt-2">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-header">
                <div class="card-body">
                    <div class="card-body d-flex justify-content-between align-items-center">
                        <h1 class="card-title mx-auto">Actividad de Usuarios</h1>
                    </div>
                    <hr />
                    <table class="table table-striped" id="tbList">
                        <thead>
                            <tr>
                                <th>Fecha de Actividad</th>
                                <th>Nombre Completo</th>
                                <th>Detalle de Actividad </th>
                            </tr>
                        </thead>
                        <tbody>
                            <!-- Table data will be dynamically inserted here by JavaScript -->
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/Activity.js"></script> 
}
